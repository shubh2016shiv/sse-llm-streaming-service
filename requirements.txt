# Core Framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.3.0

# Redis & Caching
redis[hiredis]==5.0.4  # Async Redis client with C parser
hiredis==2.3.2         # Fast Redis protocol parser

# Message Queue
aiokafka==0.10.0       # Async Kafka client

# Resilience & Rate Limiting
pybreaker==1.0.2       # Circuit breaker with Redis backend
slowapi==0.1.9         # Rate limiting for FastAPI
tenacity==8.3.0        # Retry logic with exponential backoff
aioresilience==0.2.1   # Async resilience patterns (circuit breaker, retry, etc.)

# Logging & Monitoring
structlog==24.1.0      # Structured logging
prometheus-client==0.20.0  # Metrics collection

# LLM Providers
openai==1.30.0         # OpenAI API client
google-generativeai==0.5.4  # Google Gemini
httpx==0.27.0          # Async HTTP client

# Utilities
python-dotenv==1.0.0   # Environment variables
tinydb==4.8.0          # Lightweight database (existing)
orjson>=3.10.0         # Fast JSON serialization (2-4x faster than stdlib)

# Testing
pytest==8.2.0
pytest-asyncio==0.23.0
pytest-cov==5.0.0
locust==2.23.0         # Load testing